<h3>Звіт</h3>
<h4>Загальна кількість місць для проживання станом на <%= @dt %></h4>


<table class="table">
  <% @rep.each do |area| %>
  <tr>
    <td colspan="3"><h5>Код області (коатуу): <%= area[:code] %>, <%= area[:name] %></h5></td>
  </tr>
  <tr>
    <td>Код району</td>
    <td>Назва району</td>
    <td><b>Місць</b></td>
  </tr>
  <% area[:regions].each do |region| %>
  <tr>
    <td><%= region[:code] %></td>
    <td><%= region[:name] %></td>
    <td><%= region[:qtt] %></td>
  </tr>
  <% end %>
  <tr>
    <td colspan="2" align="right">Всього:</td>
    <td><b><%= area[:qtt] %></b></td>
  </tr>
  <% end %>
</table>
