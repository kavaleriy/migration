<% if @housings && @housings.count > 0 %>
    $("#results").html("<%= escape_javascript(render partial: 'results') %>");
<% else %>
    $("#results").html("<%= escape_javascript(render partial: 'no_results') %>");
<% end %>

$("#results").removeClass('spinner').fadeIn(100);

