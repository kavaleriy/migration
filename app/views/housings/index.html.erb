<%- model_class = Housing -%>
<i class="fa fa-edit"></i> <%=t '.enter_data' %> | <%=t '.title', :default => model_class.model_name.human.pluralize.titleize %>

<%= link_to t('.new', :default => t("helpers.links.new")), new_housing_path, :class => 'btn btn-primary pull-right' %>

<div id="table">
  <%= render partial: 'table' %>
</div>

